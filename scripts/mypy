#!/usr/bin/env bash
parent_path=$( cd "$(dirname "${BASH_SOURCE[0]}")" ; pwd -P )

if [ -f "$parent_path/python" ]; then
    "$parent_path/python" -m mypy "$@"
else
    "$parent_path/../python" -m mypy "$@"
fi
